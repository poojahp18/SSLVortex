VORTEX SSL WEB DEBUGGING PROXY

The SSL web debugging proxy is a tool which is configured to trace the all ongoing HTTP and HTTPS connections and prepares a report of packet headers and data. The tracking of previous session of network can be viewed later, as the information is saved in database. In this context, the system uses SSL stream and certification authentication mechanism.
